# Copy to .env and set a local value. The API refuses to start a request
# without TOOL_API_TOKEN; there is no built-in default.
TOOL_API_TOKEN=change-me-local-dev
TOOL_WEB_ORIGIN=http://localhost:5173
